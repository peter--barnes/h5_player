//import * as rtsp from 'rtsp/rtsp_player';
import * as mp4gen from 'player/iso-bmff/mp4-generator';
import * as mse from 'player/video_presenters/mse';
import * as remuxer from 'player/remuxer/remuxer';
/*
import * as remuxer from 'player/remuxer';
import * as rtp_payload from 'player/rtp_payload';
import * as util from 'player/util';
import * as mse from 'player/video_presenters';
*/

//rtsp.RTSP_CONFIG['websocket.url'] = "ws://127.0.0.1:8080/ws";

/*
document.addEventListener("DOMContentLoaded", function() {
    setTimeout(()=> {
        let player = rtsp.attach(document.getElementById('test_video'));
        if (!player.started()) {
            player.start();
        }
    }, 200);
});
*/
